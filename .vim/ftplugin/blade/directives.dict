@each(
@inject(
@yield(
@show(
@section(
@append(
@endsection
@stop
@overwrite
@unless(
@endunless
@lang(
@choice(
@else
@for(
@foreach(
@break(
@continue(
@forelse(
@can(
@cannot(
@if(
@elseif(
@empty
@while(
@endwhile
@endfor
@endforeach
@endcan
@endcannot
@endif
@endforelse
@extends(
@include(
@stack(
@push(
@endpush